repositories {
    mavenCentral()
}

dependencies {
    implementation project(':model')
    testImplementation 'junit:junit:4.11'
}

test {
    testLogging {
        events 'started', 'passed', 'skipped', 'failed'
    }
}
